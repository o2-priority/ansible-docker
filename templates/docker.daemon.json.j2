{
{% if docker_data_root|default('false') %}
    "data-root": "{{ docker_data_root }}"
    ,"log-opts": {
      "max-size": "100m",
      "max-file": "3"
    }
{% endif %}
{% if docker_insecure_registries|default('false') %}
{% set registry_list = [] %}
{% for registry in docker_insecure_registries %}
{{ registry_list.append('\"' + registry + '\"') }}
{% endfor %}
    ,"insecure-registries": [ {{ registry_list | join(', ') }} ]
{% endif %}
}
